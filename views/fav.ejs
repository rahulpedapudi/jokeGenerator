<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <a href="/">Back</a>
    <div class="favos">
      <% list.forEach(item => { %>
      <h3>Setup: <%= item.fsetup %></h3>
      <p>Delivery: <%= item.fdelivery %></p>
      <% }); %>
    </div>
  </body>
</html>
